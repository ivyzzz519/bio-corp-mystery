# 把《生物公司杀人案》发布到网上（零基础 · 零服务器费用）

你的游戏是**纯静态网页**（HTML + CSS + JS），不需要买服务器、不需要会编程部署。下面以 **Gitee Pages** 为主方案：免费、大陆访问通常比 GitHub Pages 更顺畅。可选方案见文末。

---

## 你需要先准备什么

1. 一个 **Gitee 账号**（与 GitHub 类似，国内服务）：打开 [https://gitee.com](https://gitee.com) 注册。
2. 本文件夹 **`gameproject01`** 的完整内容（含 `index.html`、`assets`、`*.html`、`*.js` 等），不要漏文件。
3. 大约 **30～60 分钟** 第一次操作；以后更新只要重复「上传覆盖」几步即可。

---

## 一、Gitee Pages（推荐，0 元）

### 1. 新建仓库

1. 登录 Gitee → 右上角 **「+」** → **「新建仓库」**。
2. **仓库名称**：建议英文，例如 `bio-corp-mystery`（之后网址里会出现这个名字，**不要用中文仓库名**，避免 URL 里出现编码乱码）。
3. 选 **「公开」**（私有仓库一般不能免费开 Pages，以 Gitee 当时规则为准）。
4. **不要**勾选「使用 Readme 初始化仓库」（你已有完整项目，避免多一层空目录）。
5. 点 **「创建」**。

### 2. 把游戏文件上传到仓库

有两种简单方式，任选其一。

#### 方式 A：网页上传（最直观）

1. 进入刚建的仓库 → **「文件」** 标签。
2. **「上传文件」**。
3. 把本机 **`gameproject01` 文件夹里的所有内容**（不是上传外层空壳）拖进去：  
   应包含至少：`index.html`、`search.html`、`mailbox.html`、`progressTracker.js`、`assets` 文件夹（内含 `tailwind.css`、**`page-title-strip.js`** 等）、`gameData.js`、`images` 文件夹（若有）、各 `*.html` 等。
4. **填写提交说明**（例如：`首次上传游戏`）→ **「提交」**。

> 注意：仓库根目录下应直接能看到 `index.html`，不要多包一层「gameproject01/gameproject01」再上传，否则首页地址会变成 `…/gameproject01/index.html`，容易搞错路径。

#### 方式 B：Git 客户端（以后更新方便）

若你以后会经常改游戏，可安装 [Git for Windows](https://git-scm.com/download/win)，在 **`gameproject01` 目录**里打开终端，按 Gitee 仓库页给出的命令 `git init`、`git remote add`、`git add .`、`git commit`、`git push` 推送。零基础若觉得麻烦，先用方式 A 即可。

### 3. 开启 Gitee Pages

1. 根据 Gitee 当前政策，**首次使用 Pages 可能需要完成实名认证**（按网站提示操作即可，一次性）。
2. 打开仓库 → **「服务」** → **「Gitee Pages」**（有的在 **「管理」** 里，以界面为准）。
3. **部署目录**一般选 **根目录 `/`**（因为你的 `index.html` 在根目录）。
4. **分支**通常选 **`master` 或 `main`**（与你推送的分支一致）。
5. 点击 **「启动」** 或 **「更新」**。
6. 等待几分钟，页面会给出你的访问地址，形如：  
   `https://你的用户名.gitee.io/bio-corp-mystery/`  
   在浏览器打开，应能进入 **「生物公司杀人案 · 开始」**（`index.html`）。

### 4. 发布后请你自测这几项

- 点 **「开始调查」** 能否进入工作台。
- 随便打开几条新闻、登录、邮箱、云笔记是否正常。
- 结局页、清除进度页 `reset-progress.html` 是否能打开。

若样式全没了：多半是 **`assets` 或 `tailwind.css` 没上传** 或路径不在同一相对层级，回到仓库检查文件结构是否与本地一致。

### 5. 以后修改游戏怎么更新

1. 本地改好文件。
2. 再在 Gitee 网页上 **上传同名文件覆盖**，或用 Git 推送。
3. 到 **Gitee Pages** 点一次 **「更新」**（若未自动刷新），等 1～3 分钟再强制刷新浏览器（`Ctrl+F5`）。

---

## 二、网址与文件名（给玩家看的）

当前已改为 **英文路径 + 可读中文标题**：

| 打开地址（示例） | 浏览器标签页标题（用户可见） |
|------------------|------------------------------|
| `index.html` | 生物公司杀人案 · 开始 |
| `001-oa-home.html` | 员工工作台 · 公司门户 |
| `007-desk-linlan.html` | 工作台 · 林岚账号 |
| `012-desk-zhangchi.html` | 工作台 · 张弛账号 |
| `013-ending-report-to-police.html` | 结局 · 报案之后 |
| `014-ending-shadow-archive.html` | 结局 · 深层卷宗 |
| `015-ending-recap.html` | 结局 · 复盘与选择 |
| `reset-progress.html` | 清除存档 · 测试工具 |
| 其余 `002-…`～`018-…`、`search.html`、`mailbox.html` | 见各页 `<title>` |

**入口链接请发：** `https://你的域名/仓库名/`（会自动打开 `index.html`）。

---

## 三、可选：自定义域名（仍可不花钱在「域名」上，但需你已有一个域名）

1. 在域名 DNS 里按 Gitee 文档添加 **CNAME** 指向 Gitee 提供的 Pages 域名。
2. 在 Gitee Pages 设置里绑定域名并等待证书（若有 HTTPS 选项，按提示开启）。

（域名本身若需购买则不是「0 元」，但托管仍可不花钱。）

---

## 四、其他零成本方案（简要）

| 方案 | 优点 | 对大陆玩家 |
|------|------|------------|
| **Cloudflare Pages** | 免费、全球 CDN、绑定简单 | 有时不稳定，可作备用 |
| **GitHub Pages** | 免费、教程多 | 偶发访问慢或被干扰 |
| **腾讯云 / 阿里云 OSS 静态托管** | 大陆快 | 常有免费额度但规则常变，需实名，对零基础略繁琐 |

零基础优先 **Gitee Pages + 网页上传** 即可。

---

## 五、常见问题

**Q：打开是 404？**  
仓库根目录是否有 `index.html`？Pages 是否选的是根目录、正确分支？

**Q：进度条/登录状态错乱？**  
让玩家打开 `reset-progress.html` 清除后再从 `index.html` 开始。

**Q：想隐藏「清除存档」页不让普通玩家点？**  
不必把链接放在显眼入口即可；地址仍可通过 `reset-progress.html` 访问，仅作你自用测试。

---

## 六、开发者备注：若再次批量改名

本目录内曾提供脚本 `scripts/rename_pages_for_deploy.py`（一次性使用）。**不要重复执行**，除非你又改回中文文件名并需再次迁移。日常改文案直接编辑对应 `*.html` / `*.js` 即可。

祝发布顺利。若你告诉我「打算用 Gitee 还是 GitHub」，我可以按你选的平台把上文某一段改成「逐步点哪里」的截图式清单（仍保持文字版）。
